# Output Parameter Rename Plan

## 目標

將 `chrome-settings-collector.ps1` 的輸出目錄參數由 `-OutputDirectory` 改成較短的 `-Output`，並同步更新文件說明。

- [ ] 確認參數影響範圍，因為除了腳本本身，還要一起更新 README 與相關計畫檔中的舊參數名稱。
- [ ] 修改輸出參數名稱，因為希望使用時改成較短的 `-Output`，但仍保持現有輸出行為不變。
- [ ] 更新文件說明，因為 README 與相關說明都需要改成新參數名稱，避免混淆。
- [ ] 驗證新參數行為，因為要確認 `-Output` 可正常指定輸出目錄，且腳本與文件不再殘留舊名稱。

## 注意事項

- 只改參數名稱，不改報告內容或預設輸出邏輯
- 待使用者確認後再開始實作
